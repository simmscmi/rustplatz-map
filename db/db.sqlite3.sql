CREATE TABLE entries (id INTEGER PRIMARY KEY AUTOINCREMENT, title text default '', description text default '', createdAt DATETIME DEFAULT CURRENT_TIMESTAMP, col varchar(4), row varchar(4));
CREATE TABLE sqlite_sequence(name,seq);
